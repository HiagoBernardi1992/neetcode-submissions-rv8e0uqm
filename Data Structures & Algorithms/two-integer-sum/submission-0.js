class Solution {
    /**
     * @param {number[]} nums
     * @param {number} target
     * @return {number[]}
     */
    twoSum(nums, target) {
        let waitingResults = {};
        for(let num in nums) {
            if(waitingResults[target - num]){

            } else {}
        }
    }
}
